Django==4.2.16
django-location-field==2.7.3
Pillow==11.0.0
python-dotenv==1.0.1
pypaystack2==3.0.2
django-crispy-forms==2.3
crispy-bootstrap5==0.7
whitenoise==6.8.2
django-ratelimit==4.2.0
sentry-sdk==1.51.0

# Optional: Real-time features (requires separate setup on PythonAnywhere)
# channels==4.1.0
# daphne==4.1.2
# channels-redis==4.2.0

# Optional: Task queue (requires Celery or Django-Q setup)
# django-q==1.3.9
# celery==5.4.0

# Database adapters (use SQLite for development, PostgreSQL for production)
# psycopg2-binary==2.9.12